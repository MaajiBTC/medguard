# Python / Django
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
db.sqlite3
db.sqlite3-journal
ledger.sqlite3
ledger.sqlite3-journal
media/
staticfiles/
.env
backend_detached.log
backend_detached_err.log

# Node / React
node_modules/
npm-debug.log*
dist/
build/

# Encrypted local caches / offline mode (per CLAUDE.md — never commit real data)
*.enc
local_cache/

# Enrollment data — never commit real patient/staff/fingerprint data
enrollment_data/
*.fingerprint
*.template

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Hackathon submission bundle — the demo video alone is ~574 MB, far past
# GitHub's 100 MB per-file limit, and none of this belongs in the source tree.
Submission/
*.mp4
*.mov
*.mkv
*.webm

# Submission documents — kept local, not part of the source tree.
MedGuard-Technical-Writeup.docx
MedGuard-Technical-Writeup.pdf
